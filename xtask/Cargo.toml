[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

# Zero dependencies, like every other crate here. This one also has to compile
# under the `msrv` gate it defines, since that gate checks the whole workspace.
[dependencies]

[lints]
workspace = true
