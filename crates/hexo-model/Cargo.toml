[package]
name = "hexo-model"
description = "The model-package API: what every model crate provides to the container, and the checkpoint manifest and probe that hold it honest."
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
name = "hexo_model"
path = "src/lib.rs"

[dependencies]
hexo-engine.workspace = true
hexo-runner.workspace = true
hexo-search.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
